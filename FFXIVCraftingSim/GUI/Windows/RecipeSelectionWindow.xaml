<Window x:Class="FFXIVCraftingSim.GUI.Windows.RecipeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FFXIVCraftingSim.GUI.Windows"
        mc:Ignorable="d"
        Title="Select Recipe" Height="400" Width="405" WindowStyle="ToolWindow" FontFamily="Consolas" Loaded="Window_Loaded">
    <Grid>
        <TextBox x:Name="TextBoxSearchString" Padding="4,4" Height="23" TextWrapping="NoWrap" VerticalAlignment="Top" TextChanged="SearchTextChanged"/>
        <ListView x:Name="ListViewRecipes" Margin="0,23,0,0" MouseDoubleClick="MouseDoubleClickRecipe">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="50" Header="Id" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Width="80" Header="Level" DisplayMemberBinding="{Binding Level}" />
                    <GridViewColumn Width="200" Header="Name" DisplayMemberBinding="{Binding Name}" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
